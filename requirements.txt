# ============================================================
# requirements.txt — mltools Expert ML Stack
# ============================================================
# Install: pip install -r requirements.txt

# ── CORE DATA ─────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
polars>=0.20.0          # Faster alternative to pandas untuk data besar
pyarrow>=14.0           # Backend parquet untuk pandas & polars
scipy>=1.12.0
statsmodels>=0.14.0

# ── ML FRAMEWORK ──────────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.2.0
catboost>=1.2.0

# ── FEATURE ENGINEERING ───────────────────────────────────────
feature-engine>=1.6.2
category_encoders>=2.6.0
imbalanced-learn>=0.12.0

# ── EDA & PROFILING ───────────────────────────────────────────
ydata-profiling>=4.6.0  # Auto EDA report (formerly pandas-profiling)
sweetviz>=2.3.0
missingno>=0.5.2

# ── VISUALIZATION ─────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# ── TUNING & TRACKING ─────────────────────────────────────────
optuna>=3.5.0           # Hyperparameter tuning (Bayesian)
mlflow>=2.10.0          # Experiment tracking & model registry
cloudpickle>=3.0.0

# ── INTERPRETATION ────────────────────────────────────────────
shap>=0.44.0

# ── PIPELINE & PRODUCTION ─────────────────────────────────────
joblib>=1.3.0
fastapi>=0.110.0        # Model serving API
uvicorn[standard]>=0.27.0
pydantic>=2.0.0         # Data validation & schemas
pydantic-settings>=2.0.0 # Config dari .env file
ipwhois>=1.3.0          # ASN lookup for feature extraction

# ── CONFIG & UTILITIES ────────────────────────────────────────
pyyaml>=6.0             # Load ml_config.yaml
python-dotenv>=1.0.0    # Load .env file
loguru>=0.7.2           # Logging production-grade
tqdm>=4.66.0            # Progress bar

# ── DEVELOPMENT & TESTING ─────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0       # Coverage report
pytest-asyncio>=0.21.0  # Test async endpoints
httpx>=0.25.0           # Test FastAPI endpoints
black>=23.0.0           # Code formatter
ruff>=0.1.0             # Linter (pengganti flake8 + isort)
mypy>=1.7.0             # Static type checker
pre-commit>=3.5.0       # Git hooks otomatis

# ── OPTIONAL: NEURAL NETWORK ──────────────────────────────────
# Uncomment jika butuh deep learning
# torch>=2.1.0
# pytorch-tabnet>=4.1.0